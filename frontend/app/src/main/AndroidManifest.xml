<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--permissao para acesser imagens no android 13+-->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Frontend"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.cadastro.CadastroActivity"/>
        <activity android:name=".ui.main.MainActivity"/>

        <activity android:name=".ui.produto.EditarProdutoActivity" />
        <activity android:name=".ui.produto.PostarProdutoActivity"/>
        <activity android:name=".ui.propostas.ListaPropostasActivity"/>
        <activity android:name=".ui.propostas.DetalhePropostaActivity"/>
        <activity android:name=".ui.recuperacao.RecuperarSenhaActivity"/>
        <activity android:name=".ui.recuperacao.NovaSenhaActivity" android:exported="false" />
        <activity android:name=".ui.recuperacao.ConfirmarTokenActivity" android:exported="false" />


    </application>

</manifest>
